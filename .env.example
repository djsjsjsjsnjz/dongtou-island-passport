# Reserved for a later, server-side integration. Never use a VITE_ prefix for keys.
AMAP_API_KEY=
OPENTOPOGRAPHY_API_KEY=
# Optional development-only Overpass endpoint override:
OVERPASS_URL=https://overpass-api.de/api/interpreter
